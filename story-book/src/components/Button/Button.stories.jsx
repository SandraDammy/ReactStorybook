import React from "react";
import Button from "./Button";

export default {
  title: 'Button',
  component: Button
};

export const Medium = () => (
  <Button variant='medium'>Label</Button>
);

export const Large = () => (
  <Button variant='large'>Label</Button>
);

export const Small = () => (
  <Button variant='small'>Label</Button>
);

